function encode(string) {
    let vowel = {
        a: 1,
        e: 2,
        i: 3,
        o: 4,
        u: 5,
    }
    for (let char of string) {
        if ( char. )
        
    }
    
  }
  encode("hello")
  
  function decode(string) {
    return
  }